{{- if and .Values.rbac.create (not (include "garage-operator.isNamespaceScoped" .)) -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "garage-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "garage-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "garage-operator.managerRoleName" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "garage-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
